Headers for the HTML files, here, appart from html format  and some of the css code (TODO has to be moved to a script and load always the same format), is where the columns are defined, that is why there is a header for enrichment (no controls) and case control.


